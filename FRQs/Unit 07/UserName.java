// public class UserName {

// // The list of possible user names, based on a user’s first and last names and initialized by the constructor.

// private ArrayList<String> possibleNames;

// public UserName(String firstName, String lastName) {
//     for (int i = 0; i <= firstName.length() - 1; i++) {
//         System.out.println(lastName + firstName.substring(0, i)));
//     }
// }

 

// /** Returns true if arr contains name, and false otherwise. */

// public boolean isUsed(String name, String[] arr) {
//     ArrayList<String> arrList = arr.asList();
//     if(arrList.contains(name)) {
//         return true;
//     }
//     else {
//         return false;
//     }
// }

 

// /** Removes strings from possibleNames that are found in usedNames as described in part (b).

// */

// public void setAvailableUserNames(String[] usedNames) {
//     String[] arr;
//     ArrayList<String> arrList= arr.asList();
//     String name;
//     if(arrList.isUsed(String name, arr) = true) {
//         arrList.add(name);
//     }
// }

// }